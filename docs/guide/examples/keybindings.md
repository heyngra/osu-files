<!-- Generated from examples/*/index.js or index.ts. Do not edit this file directly. -->

<script setup lang="ts">
import GuideSource from '../../.vitepress/components/GuideSource.vue'
</script>

# Keybindings

Register the osu! defaults, inspect an action, and replace its assigned keys.

## manage-keybinds

Registers the default osu! bindings, prints one action, and remaps it.

[Full source](https://github.com/heyngra/osu-files/blob/main/examples/manage-keybinds/index.js)

```sh
cd examples/manage-keybinds
npm install
npm start
```

Register the built-in osu! defaults first. The result reports how many bindings were inserted and how many obsolete defaults were removed.

<GuideSource source="examples/manage-keybinds/index.js" :start-line="27" :end-line="28">

```js
const r = osu.keybindings.registerDefaults(OSU_DEFAULTS, RulesetName.Osu)
console.log(`osu! defaults - inserted: ${r.inserted}, removed: ${r.removed}`)
```

</GuideSource>

Read the keys currently assigned to `LeftButton`, then replace them with the A key and the left mouse button. String key names and `InputKey` values can be mixed.

<GuideSource source="examples/manage-keybinds/index.js" :start-line="36" :end-line="40">

```js
const keys = osu.keybindings.getActionKeys(RulesetName.Osu, 'LeftButton')
console.log(`LeftButton: ${keys.join(', ')}`)

osu.keybindings.setActionKeys(RulesetName.Osu, 'LeftButton', ['A', InputKey.MouseLeft])
console.log(`After: ${osu.keybindings.getActionKeys(RulesetName.Osu, 'LeftButton').join(', ')}`)
```

</GuideSource>

**API used:** [`init`](/api/generated/functions/init) · [`OsuFilesAPI#keybindings`](/api/generated/type-aliases/OsuFilesAPI#keybindings) · [`InputKey`](/api/generated/enumerations/InputKey) · [`RulesetName`](/api/generated/enumerations/RulesetName)
