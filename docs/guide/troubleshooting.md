# Troubleshooting

## Realm will not open

Check that the Realm path exists or is writable and that `filesFolderPath` points to the osu!lazer files folder. 

Use `readOnly: true` when inspecting a database that must not be modified.
